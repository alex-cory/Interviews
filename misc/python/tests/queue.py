# Test Queue

from data_structures.Queue import Queue

def runQueue():
    q = Queue()
    q.isEmpty()
    q.enqueue('way totally cool bros')
    q.enqueue('no freaking way')
    q.dequeue()
    q.size()
