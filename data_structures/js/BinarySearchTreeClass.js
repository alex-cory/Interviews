export class BinarySearchTree {
  constructor(tree) {
    // isBinarySearchTree(tree)
    //    this.tree = tree
  }

  isBinarySearchTree(tree) {
  }

  insert(node) {
  }

  remove(node) {
  }

  searchFor(node) {
  }
}
